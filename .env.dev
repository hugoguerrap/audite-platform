# Configuración para desarrollo local con PostgreSQL
DATABASE_URL=postgresql://audite_user:audite_password_dev@localhost:5433/audite_dev
ENVIRONMENT=development

# API Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080

# Auth (development keys - cambiar en producción)
SECRET_KEY=dev_secret_key_change_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Skip DB setup in containerized environments
SKIP_DB_SETUP=false

# Logging
LOG_LEVEL=DEBUG